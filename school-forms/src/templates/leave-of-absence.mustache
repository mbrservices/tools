<!-- Info-only entry: leave-of-absence registers no fields, so app.js hides
     the whole form/preview area and this sheet can never be shown. The file
     must still exist (every <id>.mustache is loaded unconditionally); the
     user-facing content lives in leave-of-absence.notes.html. -->
